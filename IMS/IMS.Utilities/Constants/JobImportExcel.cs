namespace IMS.Utilities.Constants
{
	/// <summary>
	/// enum constants represent columns in Excel file, with value of each enum is column order
	/// </summary>
	public enum JobColumn
	{
		No = 1,
		JobTitle,
		RequiredSkills,
		StartDate,
		EndDate,
		SalaryFrom,
		SalaryTo,
		Benefits,
		WorkingAddress,
		Level,
		Description
	}
}
